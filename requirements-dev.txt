# Dev toolchain (the lockfile substitute for the pip + venv setup chosen in M0.1).
#
# These are conservative, public-PyPI-compatible lower bounds rather than exact
# hash pins. This machine resolves packages through an internal mirror whose
# versions differ from public PyPI (where GitHub Actions installs from), so a
# frozen pin would not be portable. A fully-hashed cross-platform lock (e.g. via
# uv) can be introduced later without changing the project layout.
ruff>=0.15.20,<0.16  # kept in lockstep with the ruff-pre-commit rev in .pre-commit-config.yaml
mypy>=2.1.0
pytest>=9.1.1
pytest-cov>=7.1.0
pre-commit>=4.6.0
types-PyYAML>=6  # mypy stubs for pyyaml (M0.5)
respx>=0.21  # httpx mocking for Schwab contract tests (M1.4)

hypothesis>=6.100  # property/fuzz tests for idempotent order placement (M5.3)
