"""Trader command-line interface.

Skeleton for milestone M0.1: a placeholder ``app`` entry point so the
``trader`` console script and packaging are wired up and importable. The full
command set (run / backtest / status / reauth / kill / reconcile) is built in
milestone M0.9.
"""

from __future__ import annotations


def app() -> None:
    """Console-script entry point placeholder (replaced by a Typer app in M0.9)."""
    print("trader CLI — skeleton (M0.1). The full command set arrives in M0.9.")
